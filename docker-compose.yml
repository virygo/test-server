version: '3.8'

services:
  db:
    container_name: postgres-db
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: virygo
      POSTGRES_PASSWORD: virygo123
      POSTGRES_DB: virygo_dev
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
